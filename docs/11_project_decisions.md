# Project Decisions

Factual baseline:

- `docs/00_actual_project_audit.md`
- `docs/00_project_brief.md`
- `docs/01_architecture_map.md`
- `docs/02_unity_scene_and_object_construction.md`
- `docs/08_open_questions.md`
- `docs/09_scene_source_of_truth_decision.md`
- `docs/10_validation_triage.md`
- `README.md`
- `AGENTS.md`
- `REFACTOR_BACKLOG.md`
- `docs/assets.md`
- `docs/grok-imagine-sprite-prompts.md`

This document records accepted operating decisions for the current project baseline. It does not authorize implementation changes by itself.

## Implementation Status

Scene/build hygiene implementation status:

- The accepted Hybrid scene policy is implemented for v0.1 by tracking the five stable runtime scenes and their `.meta` files:
  - `Assets/Scenes/MainMenu.unity`
  - `Assets/Scenes/CharacterSelect.unity`
  - `Assets/Scenes/GameExploreScene.unity`
  - `Assets/Scenes/DiceBattleScene.unity`
  - `Assets/Scenes/MahjongBattleScene.unity`
- Other local scene files under `Assets/Scenes/` remain ignored unless explicitly promoted later.
- `SampleScene`, `YachtDice`, and `DiceTest` are removed from required build settings and remain cleanup targets, not runtime placeholders.
- Builders remain the authoring/regeneration mechanism for scene structure and wiring. Tracked scenes are clean-checkout runtime artifacts, not hand-authored source.
- Hold'em is now source-level implemented as a first-pass battle mode. This changes the earlier v0.1 scope assumption; it remains Unity-unvalidated until `HoldemBattleScene` is generated and tests/build/play checks pass in an isolated validation worktree.

## Decisions

| Decision | Status | Reason | Evidence | Consequence | Future revisit condition |
|---|---|---|---|---|---|
| Use a Hybrid scene policy. Stable runtime scenes must either be tracked or have a documented regeneration procedure. Experimental, obsolete, or temporary scenes are not required runtime source of truth. | Accepted | Clean checkout safety matters, but the project already has builder-owned generated scenes and no committed `Assets/Scenes/` source. Hybrid keeps a path to reproducibility without pretending all local ignored scenes are authoritative. | `docs/09_scene_source_of_truth_decision.md`, `AGENTS.md`, `README.md`, `docs/02_unity_scene_and_object_construction.md`, `.gitignore`, `ProjectSettings/EditorBuildSettings.asset` | Scene ownership must be documented per scene. Codex must not depend on ignored local scene files. Build-required scenes must either be tracked or regenerated in validation before build checks. | Revisit after scene/build hygiene task decides exactly which runtime scenes are tracked and which are generated. |
| Treat `SampleScene`, `YachtDice`, and `DiceTest` build-settings entries as cleanup targets. | Accepted | These paths are enabled in build settings but have no matching current builder and no local scene file under `Assets/Scenes/`. | `docs/00_actual_project_audit.md`, `docs/02_unity_scene_and_object_construction.md`, `docs/09_scene_source_of_truth_decision.md`, `ProjectSettings/EditorBuildSettings.asset` | They should not be documented as required runtime scenes. Cleanup should be a separate explicit ProjectSettings task. | Revisit only if a human owner restores one as an intentional tracked or generated scene. |
| Do not edit generated scene files manually. | Accepted | Scene wiring is builder-owned and uses `SceneBuilderUtility.SetField()` plus persistent listener setup. Manual scene edits can drift or be overwritten. | `AGENTS.md`, `.claude/rules/scene-builder.md`, `docs/02_unity_scene_and_object_construction.md` | Scene behavior changes must go through builders or runtime code in scoped tasks. Generated scenes are validation outputs unless intentionally tracked later. | Revisit only if the hybrid policy promotes specific stable generated scene files to tracked artifacts. |
| v0.1 scope includes Dice, Mahjong, and first-pass Hold'em playable loops in the current linear explore flow. | Accepted | Dice and Mahjong battle systems are implemented, and Hold'em now has source-level runtime rules, controller, builder, and tests. The current macro loop is `MainMenu -> CharacterSelect -> GameExploreScene -> DiceBattleScene/MahjongBattleScene/HoldemBattleScene -> GameExploreScene/MainMenu`. | `docs/00_project_brief.md`, `docs/01_architecture_map.md`, `docs/holdem_battle.md`, `Assets/Scripts/Battle/`, `Assets/Scripts/Mahjong/`, `Assets/Scripts/Holdem/`, `Assets/Scripts/Explore/GameExploreController.cs` | Stabilization must now cover Hold'em routing, scene generation, and manual QA, while avoiding additional Hold'em mechanics. | Revisit after isolated Unity validation confirms or rejects the first-pass implementation. |
| Texas Hold'em battle implementation is no longer placeholder-only. | Superseded | Earlier docs treated Hold'em as out of scope because it routed to dice and had no rules or scene. Current source routes Hold'em to `HoldemBattleScene` and includes Hold'em rule/model tests. | `Assets/Scripts/Holdem/`, `Assets/Editor/HoldemBattleSceneBuilder.cs`, `Assets/Editor/Tests/Holdem/`, `Assets/Scripts/Explore/GameExploreController.cs`, `docs/holdem_battle.md` | Do not re-document Hold'em as missing. Treat it as source-level implemented but Unity-unvalidated until validation completes. | Revisit after the generated scene and build settings policy for `HoldemBattleScene` is finalized. |
| Slay the Spire-style node map is out of scope for v0.1. | Rejected for v0.1 | Current progression is a linear stage-round model, not a graph/node-map system. | `docs/00_actual_project_audit.md`, `docs/01_architecture_map.md`, `docs/08_open_questions.md`, `Assets/Scripts/Stages/StageData.cs` | Do not add route graph data, node-map UI, map persistence, or route-selection systems for v0.1. | Revisit after the linear loop is stable and the product owner explicitly requests macro-progression expansion. |
| Shop, relic, joker, and card-modifier systems are out of scope for v0.1. | Rejected for v0.1 | The implemented reward layer is item-box `PowerUpType`; no shop/relic/joker systems were found. | `docs/00_actual_project_audit.md`, `docs/00_project_brief.md`, `docs/08_open_questions.md`, `Assets/Scripts/Game/PowerUpType.cs`, `Assets/Scripts/Explore/GameExploreController.cs` | Do not introduce new economy, relic, joker, deck, or shop architecture in v0.1 cleanup. | Revisit as a post-v0.1 product design decision. |
| Explicit shared Balatro-style chips/mult scoring is out of scope for v0.1. | Rejected for v0.1 | Dice and Mahjong use separate scoring/damage systems. No shared chips/mult abstraction exists. | `docs/00_actual_project_audit.md`, `docs/00_project_brief.md`, `docs/08_open_questions.md`, `DamageCalculator`, `YakuEvaluator`, `MahjongDamageTable` | Do not add shared score architecture while stabilizing existing combat loops. Avoid documenting chips/mult as implemented. | Revisit only after dice and mahjong stabilization, if product design calls for a shared scoring layer. |
| Broad `SceneBuilderUtility` or SceneBuilder refactor is out of scope for v0.1. | Rejected for v0.1 | Scene builders are high-risk, string-wired, and central to generated scenes. The backlog recommends one focused extraction at a time only after validation is stable. | `REFACTOR_BACKLOG.md`, `AGENTS.md`, `docs/02_unity_scene_and_object_construction.md` | Do not split builder architecture broadly during v0.1 stabilization. Builder edits should be limited to scene/build hygiene or explicitly scoped fixes. | Revisit after validation baseline is clean and a single focused builder extraction has tests or verification steps. |
| Existing failing EditMode tests must be fixed or explicitly classified before feature expansion. | Accepted | Current focused triage found failing tests in battle positioning, projectile timing, and Mahjong yaku behavior. | `docs/10_validation_triage.md`, `AGENTS.md`, `REFACTOR_BACKLOG.md` | Feature work waits until failures are either corrected or documented as accepted baseline behavior. | Revisit after all EditMode tests pass, or after a documented waiver exists for each remaining failure. |
| Unity validation must run in a separate validation worktree. | Accepted | Foreground checkout must not be used for automated Unity tests/builds. Validation can dirty project settings or generated files. | `AGENTS.md`, `docs/10_validation_triage.md` | Automated tests/builds use detached or dedicated validation worktrees, with logs/results/build outputs outside project root. | Revisit only if a formal CI workflow replaces local worktree validation while preserving isolation. |
| For command-line EditMode tests in the observed setup, omit `-quit` and let Unity Test Framework exit after `-runTests`. | Accepted | The triage run with `-quit` imported and exited without result XML; runs without `-quit` produced results. | `docs/10_validation_triage.md` | Validation docs and commands should avoid `-quit` for `-runTests` unless the behavior changes. | Revisit after Unity/Test Framework upgrade or if a new command pattern reliably produces results with `-quit`. |
| Treat Unity-ready transparent PNGs, required source images, and prompt/provenance docs as source-of-truth candidates. | Accepted | Current builders and runtime load PNG sprites and documented asset paths. Prompt/provenance docs explain generation intent. | `docs/assets.md`, `docs/grok-imagine-sprite-prompts.md`, `docs/00_actual_project_audit.md` | Asset policy work should preserve these files until a manifest classifies them more precisely. Do not delete assets in baseline tasks. | Revisit when `docs/04_content_and_asset_pipeline.md` or an asset manifest explicitly classifies each asset category. |
| Treat MP4s, extracted raw frames, and rembg intermediate outputs as intermediate artifacts unless explicitly promoted later. | Accepted | The audit found MP4s, raw frame folders, `_transparent`, `_transparent_clean`, and `_764x640` outputs, but no tracked repeatable extraction/rembg scripts. | `docs/00_actual_project_audit.md`, `docs/assets.md`, `docs/grok-imagine-sprite-prompts.md` | Do not assume every generated/intermediate asset is permanent source of truth. Do not delete or move them in this task. | Revisit after an asset pipeline manifest identifies source, intermediate, and Unity-ready outputs. |
| Do not delete assets during baseline stabilization. | Accepted | Asset provenance is only partially documented, and many builder/runtime paths are hard-coded. | `docs/00_actual_project_audit.md`, `docs/02_unity_scene_and_object_construction.md`, `docs/assets.md`, `AGENTS.md` | Cleanup tasks may classify assets but must not delete them without explicit approval. | Revisit after asset manifest and Unity import/path validation are complete. |
| Use documentation as the stable factual baseline, but do not treat recommendations in older docs as final when superseded here. | Accepted | `docs/09_scene_source_of_truth_decision.md` recommended generated-only scenes, while this baseline accepts Hybrid. | `docs/09_scene_source_of_truth_decision.md`, current owner instruction | This file supersedes older recommendations where it records a newer accepted decision. Older evidence remains valid. | Revisit when source docs are consolidated or updated to remove stale recommendations. |
